Fixed
- Fix stuttering and crashes on devices with 500+ apps
- Move app loading off the main thread to prevent UI freezing during startup
- Reduce memory usage by caching bitmaps and batching list operations
- Fix app icon bitmaps being re-created on every scroll, causing jank
- Fix icon packs dropdown showing only "None"
- Pre-warm app icon bitmaps in background to prevent frame drops during scroll
- Cache icon painters and hoist preferences to reduce per-item overhead
- Fix crash when refreshing icons with many installed apps
